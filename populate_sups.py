import sys

def populate():
    print("ℹ Note : La table superviseur_info a été remplacée par une Vue dynamique (vw_superviseur_info).")
    print("Ce script (populate_sups.py) n'est plus nécessaire. Veuillez ne plus l'utiliser.")
    print("La vue se met à jour automatiquement avec les nouvelles données de agent_perf_info.")

if __name__ == '__main__':
    populate()
